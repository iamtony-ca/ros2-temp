# Isaac Sim 강화학습 — 발표 및 보고 자료 (30분 · Part 4·5·6 중심)

> **강의 제목:** Isaac Sim 강화학습 (PinkLAB)
> **발표 시간:** 약 30분 · **대상:** 혼합 청중(기술팀 + 비개발자)
> **초점:** 핵심은 **Part 4(강화학습)·5(커스텀 로봇 통합)·6(휴머노이드 RL)** — 여기에 시간·설명을 집중. Part 1–3은 빠른 도입으로 압축.

---

## ⏱️ 시간 배분 가이드 (총 30분) — 핵심(4·5·6)에 약 22분 집중

| 구간 | 내용 | 소요 |
|------|------|------|
| 1 | 도입 — 왜 시뮬레이션 기반 강화학습인가 | 3분 |
| 2 | Isaac Sim / Isaac Lab 개요 + Part 1–3 초고속 요약 | 5분 |
| 3 | **Part 4: 강화학습 환경과 학습 (최핵심)** | 10분 |
| 4 | **Part 5: 커스텀 로봇 통합 (URDF→USD, ROS2)** | 6분 |
| 5 | **Part 6: 휴머노이드 강화학습 (G1)** | 4분 |
| 6 | 핵심 정리 & 활용 방안 | 2분 |

> 💡 발표 팁: 앞부분(1–2)은 "왜/무엇"만 빠르게 던지고, **Part 4에서 호흡을 길게** 가져가세요. 청중의 관심이 가장 큰 구간입니다.

---

## 1. 도입 — 왜 "시뮬레이션 기반 강화학습"인가 (3분)

- 실물 로봇 학습은 **느리고·위험하고·비싸다.** 로봇이 넘어지고 부딪히는 수만 번의 시행착오를 실물로 감당 불가.
- **해결책 = 가상에서 먼저 수천 번 학습 → 실물로 이전(Sim-to-Real).**

> *"현실에서 부딪히기 전에, 가상에서 수천 번 넘어져 보게 하자."*

---

## 2. Isaac Sim / Isaac Lab 개요 + Part 1–3 초고속 요약 (5분)

### Isaac Sim / Isaac Lab (2분)
- **Isaac Sim**: NVIDIA 고정밀 로봇 시뮬레이터 (USD 기반, GPU 물리엔진).
- **Isaac Lab**: 그 위에서 강화학습을 쉽게 구성하는 프레임워크.
- **핵심 강점 — GPU 병렬 시뮬레이션**: 한 GPU에서 **수천 개 환경 동시 학습**(`--num_envs 1024`). 학습은 `--headless`로 대량 실행, **TensorBoard**로 모니터링.

> 💡 비유: "게임 엔진 안 수천 개의 로봇 복제본이 동시에 연습 → 가장 잘하게 된 '두뇌'만 꺼내온다."

### Part 1–3 — 기초는 "도구 확보" 단계로 요약 (3분)
강화학습에 들어가기 전, 로봇을 시뮬레이션에서 **움직이고·관측하는** 도구를 갖추는 과정.

| 파트 | 한 줄 요약 |
|------|-----------|
| Part 1 (01–04) | 시뮬레이터 실행·도형 스폰·물리 검증·**시뮬레이션 루프**(RL 루프의 토대) |
| Part 2 (05–14) | 로봇 제어(위치/토크·중력보상), 센서(카메라·LiDAR·접촉), 운동학(FK/IK), Pick&Place |
| Part 3 (15–19) | 모바일 로봇 경로추종·목표점 내비게이션·장애물 회피 |

> ⏩ 이 구간은 "이런 기초 위에서 아래 핵심이 돌아간다"만 짚고 넘어갑니다.

---

## 3. Part 4: 강화학습 환경과 학습 — **최핵심 (10분)** ⭐⭐⭐

### 3-1. 강화학습 환경의 구조 (20–23) — 4분

Isaac Lab은 RL 환경을 **`ManagerBasedRLEnv`** 표준 구조로 만든다. MDP(마르코프 결정 과정)를 "매니저"로 분리:

| 매니저 | 역할 | 예 |
|--------|------|-----|
| **Action** | 로봇에게 줄 명령 | 토크 / 위치 / 속도 |
| **Observation** | 에이전트가 보는 값 | 관절각, 속도, 자세 |
| **Reward** | 잘하면 주는 보상 | 똑바로 서기, 목표 추종 |
| **Termination** | 종료 조건 | 넘어짐, 시간 초과 |
| **Event** | 리셋·랜덤화 이벤트 | 초기 상태 무작위화 |

- **20 base_env**: 매니저 구조의 기본형.
- **21 관측·보상 설계 / 22 행동 공간(Effort·Position·Velocity) / 23 커스텀 환경 완성**: 완전한 RL 환경을 직접 조립.

> 💡 **강화학습의 90%는 "환경·보상 설계".** 무엇에 보상을 주느냐가 로봇의 행동을 결정한다. (같은 로봇도 보상 설계에 따라 전혀 다르게 행동)

### 3-2. 실제 학습 (24–29) — 6분

- **24 Cartpole PPO 학습**: **RSL-RL**의 PPO 알고리즘으로 첫 학습. `OnPolicyRunner`가 학습을 돌리고 체크포인트 저장, TensorBoard로 학습 곡선 확인. (`logs/rsl_rl/<실험명>`)
- **25 정책 평가**: 학습된 모델 로드 → 에피소드 실행 → 성능 확인.
- **26 보행 학습 / 27 보행 평가**: 다리 로봇(ANYmal) 보행 정책 학습·추론.
- **28 지형 생성 + 도메인 시프트**: 평지에서 배운 정책을 **거친 지형**에 그대로 적용 → **일반화의 한계를 직접 관찰**. (핵심 교훈: 학습 환경과 다르면 실패한다)
- **29 도메인 랜덤화**: 로봇/환경 파라미터를 무작위로 흔들어 학습 → **Sim-to-Real 전이의 핵심 기법.** 시뮬레이션과 현실의 차이(reality gap)를 견디는 정책을 만든다.

> **Part 4의 교훈 3가지**
> 1. 학습은 GPU 병렬(수천 환경)로 빠르게.
> 2. 평지에서 잘한다고 험지에서 잘하는 게 아니다(28강).
> 3. 그래서 **도메인 랜덤화**로 "웬만한 변화에도 견디는" 정책을 만든다(29강).

> 🗣️ 발표 포인트: 이 세 교훈은 곧 "우리 로봇을 실물에 적용할 때 그대로 마주칠 문제"입니다 → 다음 Part 5로 연결.

---

## 4. Part 5: 커스텀 로봇 통합 — Pinky Pro **(6분)** ⭐⭐

**우리(사용자) 로봇**을 시뮬레이션·강화학습 파이프라인에 넣는 실전 과정. 적용의 실질적 관문.

### 4-1. 에셋 파이프라인 (30–31) — 2분
- **30 URDF 준비**: 로봇 설계 파일(URDF)·메시·충돌 지오메트리 구성.
- **31 URDF→USD 변환**: `UrdfConverter`로 로봇을 Isaac Sim 포맷(USD)으로 변환.
- → **"우리 로봇 도면을 시뮬레이터가 이해하는 형태로 바꾸는" 단계.**

### 4-2. 제어·센서 (32–33) — 2분
- **32 Pinky 제어**: 차동 구동 제어, 웨이포인트 내비게이션.
- **33 센서 통합**: IMU·인코더 등 커스텀 로봇 센서 연동.

### 4-3. ROS2 브릿지 + 자율주행 (34) — 2분
- **34 ROS2(Jazzy) 브릿지**: **Gazebo 대체.** `/clock /tf /odom /joint_states /scan` 발행 + `/cmd_vel` 구독.
- **34-1 SLAM + Nav2**: `slam_toolbox`로 8×8 미로 **지도 작성 → 저장 → AMCL 자율주행** (또는 동시 SLAM 주행). `pinky_slam_nav` ROS2 패키지 포함.

> **의미:** 교육용 예제 로봇이 아니라 **실제 우리 로봇**을 넣어, 시뮬레이션 → ROS2 → 자율주행까지 잇는 실전 배선. **로봇 PoC의 직접적 청사진.**

---

## 5. Part 6: 휴머노이드 강화학습 — G1 **(4분)** ⭐⭐

이족보행 휴머노이드(Unitree G1)에 강화학습 적용 — 난이도 최고, 최신 트렌드.

### 5-1. G1 Standing (서기) — 35
- 빈 환경 랜덤 액션 관찰(35-1) → **PPO 학습**(35-2) → 정책 평가(35-3).
- 환경 정의는 공유 모듈 `g1_standing_env.py`. "넘어지지 않고 서 있기"부터.

### 5-2. G1 Walking (걷기 = 모션 모방/imitation) — 36
- 사람의 **레퍼런스 모션 재생**(36-1) → **RSI(Reference State Initialization) + PPO**로 그 동작을 모방 학습(36-2) → 평가(36-3).
- 레퍼런스 모션 데이터: HuggingFace `openhe/g1-retargeted-motions`.
- 즉, "맨땅에서 걷기를 발명"하는 대신 **사람 동작을 흉내 내며** 자연스러운 보행을 학습.

> 💡 시사점: 최신 휴머노이드 학습의 핵심 트렌드(**모션 모방 기반 강화학습**)를 실습으로 경험. 향후 휴머노이드/복잡 동작 과제의 방법론적 토대.

---

## 6. 핵심 정리 & 활용 방안 (2분)

### 확보 역량 (핵심 4·5·6 기준)
1. **RL 환경 설계** — MDP(관측/행동/보상/종료)를 표준 구조로 조립 (Part 4).
2. **대규모 병렬 학습 + Sim-to-Real** — PPO(RSL-RL), 도메인 랜덤화 (Part 4).
3. **커스텀 로봇 통합** — 우리 로봇을 URDF→USD→ROS2→자율주행으로 연결 (Part 5).
4. **고난도 동작 학습** — 휴머노이드 모션 모방 RL (Part 6).

### 활용 아이디어 (논의용)
- 자사 로봇 제어 정책을 **실물 시험 전 시뮬레이션에서 사전 검증** → 리스크·비용 절감 (Part 4·5).
- 위험/반복 작업(자율주행·물체 조작)의 **정책을 가상에서 먼저 학습** (Part 4·5).
- **디지털 트윈**으로 현장 환경을 재현해 시나리오 테스트 (Part 5).
- 복잡한 동작(휴머노이드·매니퓰레이션)은 **모션 모방 방식** 검토 (Part 6).

### 다음 단계 제안
- 로봇 1종 선정 → **Part 5 파이프라인(URDF→USD→ROS2)** 으로 시뮬레이션 통합 PoC.
- 소규모 과제(예: 목표점 내비게이션)로 **Part 4 방식 강화학습 PoC** 후 성과 공유.

---

## 마무리 (Q&A)

- **한 줄 결론:** *"Part 4·5·6을 통해, 우리 로봇을 현실에 내보내기 전 가상에서 학습·통합·검증하는 전 과정을 확보했다."*
- 질의응답.

> 부록: 강좌 전체 목차·실행법은 `isaacsim_tutorials/README.md` 참고.
