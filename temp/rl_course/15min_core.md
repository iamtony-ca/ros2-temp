# Isaac Sim 강화학습 — 발표 및 보고 자료 (15분 요약 · Part 4·5·6 중심)

> **강의 제목:** Isaac Sim 강화학습 (PinkLAB)
> **발표 시간:** 약 15분 · **대상:** 혼합 청중(기술팀 + 비개발자)
> **초점:** 핵심은 **Part 4(강화학습)·5(커스텀 로봇 통합)·6(휴머노이드 RL)**. 도입·기초는 최소화. (상세본: 30분 핵심강조 자료 참고)

---

## ⏱️ 시간 배분 가이드 (총 15분) — 핵심(4·5·6)에 약 11분 집중

| 구간 | 내용 | 소요 |
|------|------|------|
| 1 | 왜 시뮬레이션 RL + Isaac Sim/Lab 개요 | 2분 |
| 2 | 기초(Part 1–3) 30초 요약 | 1분 |
| 3 | **Part 4: 강화학습 (최핵심)** | 6분 |
| 4 | **Part 5: 커스텀 로봇 통합** | 3분 |
| 5 | **Part 6: 휴머노이드 RL** | 2분 |
| 6 | 핵심 정리 & 활용 | 1분 |

---

## 1. 왜 시뮬레이션 RL + Isaac Sim/Lab 개요 (2분)

- 실물 로봇 학습은 **느리고·위험하고·비싸다** → **가상에서 먼저 수천 번 학습 후 실물 이전(Sim-to-Real).**
- **Isaac Sim**(NVIDIA 고정밀 시뮬레이터) + **Isaac Lab**(RL 프레임워크).
- **핵심 강점 — GPU 병렬 시뮬레이션**: 한 GPU에서 **수천 개 환경 동시 학습**(`--num_envs 1024`), `--headless` 대량 실행, **TensorBoard** 모니터링.

> 💡 비유: "게임 엔진 안 수천 개 로봇 복제본이 동시에 연습 → 가장 잘하게 된 '두뇌'만 꺼내온다."

---

## 2. 기초(Part 1–3) 30초 요약 (1분)

강화학습 전, 로봇을 시뮬레이션에서 **움직이고·관측하는 도구 확보** 단계.
- Part 1: 시뮬레이터 실행·시뮬레이션 루프(RL 루프의 토대)
- Part 2: 로봇 제어·센서(카메라/LiDAR/접촉)·운동학(FK/IK)
- Part 3: 모바일 로봇 내비게이션·장애물 회피

> ⏩ "이 기초 위에서 아래 핵심이 돌아간다"만 짚고 통과.

---

## 3. Part 4: 강화학습 — **최핵심 (6분)** ⭐⭐⭐

### (1) 환경 설계 (20–23)
Isaac Lab은 RL 환경을 **`ManagerBasedRLEnv`** 표준 구조로 만든다. MDP를 매니저로 분리:

| 매니저 | 역할 |
|--------|------|
| Action | 로봇에게 줄 명령(토크/위치/속도) |
| Observation | 에이전트가 보는 값(관절각·속도·자세) |
| Reward | 잘하면 주는 보상 |
| Termination | 종료 조건(넘어짐·시간초과) |

> 💡 **강화학습의 90%는 "환경·보상 설계".** 무엇에 보상을 주느냐가 로봇 행동을 결정.

### (2) 학습 (24–29)
- **PPO(RSL-RL)** 로 학습 → 체크포인트 저장 → 평가. (Cartpole → 보행 로봇 ANYmal)
- **28 도메인 시프트**: 평지 정책을 험지에 적용 → **일반화 한계 관찰**.
- **29 도메인 랜덤화**: 파라미터를 흔들어 학습 → **Sim-to-Real 전이 핵심** (현실 차이를 견디는 정책).

> **교훈:** ① 병렬로 빠르게 학습, ② 평지에서 잘해도 험지에선 실패, ③ 그래서 도메인 랜덤화로 견고하게. → 실물 적용 시 그대로 마주칠 문제(다음 Part 5로 연결).

---

## 4. Part 5: 커스텀 로봇 통합 — Pinky Pro **(3분)** ⭐⭐

**우리 로봇**을 파이프라인에 넣는 실전 = 적용의 관문.
- **30→31 에셋 파이프라인**: 로봇 도면(URDF) → Isaac Sim 포맷(USD) 변환.
- **32→33 제어·센서**: 차동구동 제어·웨이포인트 내비게이션, IMU·인코더 연동.
- **34 ROS2(Jazzy) 브릿지**: **Gazebo 대체.** `/tf /odom /scan` 발행 + `/cmd_vel` 구독.
  - **34-1 SLAM+Nav2**: 8×8 미로 **지도작성 → 저장 → AMCL 자율주행** (`pinky_slam_nav` 패키지).

> **의미:** 시뮬레이션 → ROS2 → 자율주행까지 잇는 실전 배선 = **로봇 PoC의 직접적 청사진.**

---

## 5. Part 6: 휴머노이드 RL — G1 **(2분)** ⭐⭐

이족보행 휴머노이드(Unitree G1), 난이도 최고·최신 트렌드.
- **35 서기(Standing)**: PPO로 "넘어지지 않고 서기" 학습 → 평가.
- **36 걷기(Walking) = 모션 모방**: 사람 **레퍼런스 모션 재생 → RSI + PPO로 모방 학습 → 평가.**
  - "맨땅에서 걷기 발명" 대신 **사람 동작을 흉내 내며** 자연스러운 보행 학습.

> 💡 시사점: 최신 휴머노이드 학습의 핵심(**모션 모방 기반 RL**)을 실습으로 경험 → 향후 복잡 동작 과제의 방법론적 토대.

---

## 6. 핵심 정리 & 활용 (1분)

**확보 역량:** RL 환경 설계(P4) · 대규모 병렬 학습+Sim-to-Real(P4) · 커스텀 로봇 통합(P5) · 휴머노이드 모션 모방(P6).

**활용 아이디어**
- 자사 로봇 정책을 **실물 시험 전 시뮬레이션 사전 검증** → 리스크·비용 절감.
- 위험/반복 작업 정책을 **가상에서 먼저 학습**, **디지털 트윈**으로 시나리오 테스트.
- 복잡 동작은 **모션 모방 방식** 검토.

**다음 단계:** 로봇 1종 선정 → **Part 5 파이프라인**으로 시뮬레이션 통합 PoC → **Part 4 방식** 강화학습 PoC 성과 공유.

> **한 줄 결론:** *"Part 4·5·6을 통해, 우리 로봇을 현실에 내보내기 전 가상에서 학습·통합·검증하는 전 과정을 확보했다."*

---

*상세본: `isaacsim_강화학습_발표자료_30min_핵심강조.md`. 강좌 전체 목차·실행법: `isaacsim_tutorials/README.md`.*
